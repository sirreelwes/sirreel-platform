# SirReel HQ — Shiplog

Append-only record of shipped changes. Newest at top. Each entry: SHA, commit subject, why-it-matters one-liner.

## 2026-06-13

- `6f5f078` — feat(orders): PickList sync on post-BOOKED line edits (Phase 1 step 3 of 4) — closes the PARKING LOT comment at `bookOrder.ts:191-196` ("post-book line-item edits do NOT currently propagate into the PickList") with both ADD (#3a) and DELETE (#3b) paths wired. **Shared helper** at `src/lib/orders/pickListSync.ts` — `syncPickListOnLineAdd`, `readPickListItemForDelete`, `syncPickListOnLineDelete`. The ADD helper IMPORTS `routeDepartment` from `bookOrder.ts` directly (no forked logic — the lane/pickStatus assignment for a new line is byte-identical to what the original book transition stamps; both code paths converge through one function). **#3a (ADD) — three timing cases handled**: (a) order has no PickList (booked with zero warehouse lines previously) → create one in DRAFT and add the new item; (b) PickList exists in DRAFT or PICKING → append PickListItem; (c) PickList already terminated (READY_TO_STAGE, STAGED, LOADED) — the at-pickup sandbag case → **append anyway, never error**. The list's status is preserved — we do NOT silently rewind to PICKING when an item lands on an already-staged list; the warehouse team sees one PENDING_PICK item on a list they thought was done and physically pulls it. New lines always land as `pickStatus: PENDING_PICK` (the existing enum value for "not yet picked"); no auto-pick. The POST handler stamps `fulfillmentLane`, `pickStatus`, AND records the sync outcome on the existing `order.line_item_added` AuditLog row (`fulfillmentLane`, `pickStatus`, `pickListAction: 'none' | 'appended' | 'created'`). **FLEET and STAGE lines** (VEHICLES, STAGES departments) stamp `fulfillmentLane` but skip the picking-floor side — they have no PickList participation. **#3b (DELETE) — confirm-require + explicit cascade replacement**: the DELETE handler now reads an optional JSON body (`{confirmedPicked: true}`) and, when `pickStatus ∈ {PICKED, STAGED, LOADED}` AND `confirmedPicked !== true`, returns **HTTP 409** with `{requiresConfirmation: true, reason: 'already_picked', pickStatus, physicalAction: 'return_to_stock', message: "This item was already <picked|staged|loaded> — deleting it removes it from the order. Return the physical item to stock before confirming."}`. The message names the **physical consequence** — not a generic "are you sure" — so the rep knows what they're committing to. On confirmed delete (or for PENDING_PICK / non-warehouse lines), the helper executes an EXPLICIT PickListItem delete BEFORE the OrderLineItem delete — replacing the schema's silent `onDelete: Cascade` so the deletion is an action, not a side-effect. For already-picked items, a separate AuditLog row with `action: 'picklist.item_picked_then_removed'` lands BEFORE the cascade, capturing `pickedById`, `pickedAt`, `scannedCode`, `pickListId`, `pickStatus` in `oldValues` — the un-pick is tracked, not silently dropped. Step 1's existing `order.line_item_removed` row (with pickStatus + fulfillmentLane already preserved on the OrderLineItem side) is unchanged — we lean on it for the order-grain audit and add the picklist-grain audit on top. **PickList status recompute**: conservative — only fires when the deletion empties the list (status → `CANCELLED`); partial removal leaves status untouched and the operator advances via the existing `/api/picklists/[id]/complete-picking` flow. The DELETE response now includes `pickList: { action, recomputed, wasPicked }` so the UI can render a contextual toast. **Verified end-to-end** via rolled-back transaction against the real Prisma client (no prod mutations — sentinel-thrown rollback confirmed via post-tx state read): (1) ADD a PRO_SUPPLIES line → `fulfillmentLane=WAREHOUSE`, `pickStatus=PENDING_PICK`, PickListItem appended to existing DRAFT list, audit row recorded the sync outcome; (2) DELETE that PENDING_PICK line → PickListItem cleanly removed, recompute=`unchanged` (other items remain), no un-pick audit emitted (line was never picked); (3) Create + mark PICKED with `scannedCode='TEST-SCAN'` + delete → `picklist.item_picked_then_removed` AuditLog row emitted with full picker stamp preserved in `oldValues`, PickListItem deleted. Pre-state and post-rollback PickList state byte-identical. `tsc --noEmit` clean.
- `8535ec3` — feat(orders): bookedTotal tracks live edits post-BOOKED (Phase 1 step 2 of 4) — path A of the billing-basis fix from STEP 0. **`recalcOrderTotals` (src/lib/orders.ts) now mutates `bookedSubtotal`/`bookedTaxAmount`/`bookedTotal` in lockstep with `subtotal`/`taxAmount`/`total` when the order is in the post-BOOKED mutable window**: `BOOKED`, `LOADED_READY`, `ON_JOB`, `RETURNED`, `LD_CHECK`. The existing locked-order guard (INVOICED + CLOSED bail at the top of the function) is unchanged — terminal billing states stay byte-identical to what was invoiced. APPROVED orders haven't booked yet (`bookedAt` is null until the BOOKED transition); they fall through with no booked* write. CANCELLED is terminal and excluded explicitly so a defensive recompute can't shift its snapshot. **The "three canonical money numbers" semantics shift slightly**: bookedTotal becomes "the order's current locked-in commitment value" instead of "the at-book historical snapshot." The historical at-book value is preserved on the existing `bookOrder.ts:227-234` AuditLog row (`action: 'order.booked'`, `newValues.bookedTotal`), plus every subsequent line-item edit lays down an `order.line_item_added/_updated/_removed` row from the Phase 1 step 1 helper. Audit trail is complete — the historical record didn't move, it moved tables. **Invoice impact (the whole point)**: the generator's `drift = bookedSubtotal - liveSubtotal` calculation now lands at 0 for any post-edit invoice because bookedSubtotal tracks liveSubtotal. The ADJUSTMENT line that previously REVERSED post-BOOKED edits ($500 sandbag → ADJUSTMENT of -$500 → sandbag not billed) is suppressed. The added line now actually bills. **End-to-end verified before commit** against both ratified scenarios: (1) FLAT_TOTAL pin (S260609-001 shape, target $2,500) — adding a $500 line keeps the total pinned at $2,500 (FLAT_TOTAL absorbs the addition; live-pin math in `discountedTotals.ts` is unchanged); no ADJUSTMENT line; sandbag visible on the invoice. (2) PLAIN order ($1,000 + 8.75% tax = $1,087.50 baseline) — adding a $500 line lands the invoice at $1,631.25 with no ADJUSTMENT; sandbag bills at full $500 + tax. **Reader audit**: four downstream consumers of `Order.bookedTotal` — `jobs/[id]/route.ts:218` (job rollup, uses `bookedTotal ?? subtotal` as "locked-in dollars," semantically wants current-value), `generateRentalInvoice.ts:148` (anchor — wants current-value per this commit's intent), `orders/[id]/page.tsx:2347` (canGenerate gate, only checks non-null), `jobs/[id]/page.tsx:114` (display). None treat bookedTotal as a frozen-at-booking historical record. **APPROVED island fix (rolled in)**: the audit-helper's `AUDITED_STATUSES` set gains `APPROVED` to kill the editable-but-not-audited gap from step 1. APPROVED is treated as pre-book editable everywhere else in the codebase (`dates/apply` PRE_BOOK_STATUSES, `orders/[id]` postBooking UI gate, `jobs/route.ts` rollup) — the original `isEditable = ["DRAFT", "QUOTE_SENT"]` exclusion was an arbitrary cutoff inconsistent with the semantic. Edits from APPROVED onward deviate from what the client just signed on the portal; they MUST leave a paper trail. The two line-item route handlers (`[lineId]/route.ts` PUT + DELETE) widen their pre-edit-snapshot gates to match — APPROVED now captures `oldValues` / fires audit. `tsc --noEmit` clean.
- `77e243b` — feat(orders): audit-log line-item edits after APPROVED (Phase 1 step 1 of 4) — pure instrumentation, zero behavior change. Foundation for the post-BOOKED editability gate being category-scoped over the next three commits. **Shared helper** at `src/lib/orders/auditLineItemEdit.ts` (`auditLineItemEdit`, `shouldAuditLineItemEdit`, `resolveOperatorId`, `extractIp`) mirrors the AuditLog shape already used by `bookOrder.ts:214` and `dates/apply/route.ts:218` so the AuditLog stream stays uniform across order-lifecycle events. **Audited statuses**: `BOOKED, LOADED_READY, ON_JOB, RETURNED, LD_CHECK, INVOICED, CLOSED`. **Skipped**: `DRAFT, QUOTE_SENT, APPROVED, CANCELLED` — DRAFT/QUOTE_SENT are pre-commitment quote churn (no audit per ratification), APPROVED is the gate threshold itself, CANCELLED is terminal and shouldn't reach the endpoint. **Three actions**: `order.line_item_added` (POST), `order.line_item_updated` (PUT), `order.line_item_removed` (DELETE). **POST** captures the new line shape on `newValues` (id, description, department, qty, rate, billableDays, rateType, lineTotal, inventoryItemId, assetCategoryId, package flags). **PUT** snapshots the pre-update row, runs the existing update, then computes a compact field-level diff so the AuditLog row is grep-friendly ("show me every line where rate changed last month"); only fields that actually changed land in `oldValues`/`newValues`. **DELETE** snapshots the row BEFORE the cascade-delete fires (once `orderLineItem.delete` runs the row is gone and the silent `PickListItem` cascade would lose the picker-stamp metadata too) — the audit row preserves `pickStatus` and `fulfillmentLane` so the LATE-STAGE-DELETE case (line was already PICKED) is grep-able after the fact. **Non-fatal**: every audit write is wrapped in try/catch so a failed audit row never blocks a successful edit; failures log to console for the audit-completeness cron (Phase 2.5 follow-up) to catch. **Operator id**: resolved from session.user.email via `resolveOperatorId` (returns null on miss — AuditLog.userId is nullable, so a missed lookup falls back to a "no-user" row rather than failing). **ipAddress**: extracted from `x-forwarded-for` / `x-real-ip` headers per the existing `bookOrder` pattern. **Behavior at every existing call site**: byte-identical for DRAFT/QUOTE_SENT/APPROVED orders (no audit, no extra read). For BOOKED+ orders the only added work is one extra SELECT (status + the pre-edit line snapshot) plus one INSERT on AuditLog — measured cost is negligible vs. the existing recalcOrderTotals call. `tsc --noEmit` clean.
- `cc4588f` — fix(orders): make Resend Portal Link failures visible — UX-only fix to the order detail page after Jose's "click, see nothing" report. **Endpoint untouched** per ratification; the 4xx returns were correct, just landing in the quiet `agreementMsg` info-strip where reps were missing them. **Pre-flight precondition derivation** on the page: mirrors the endpoint's three gates ([1] `!order.bookingId` → "Needs a booking before a portal link can be sent.", [2] `paperworkRequests.count === 0` → "No paperwork request yet — send the rental agreement first to mint a portal token.", [3] no recipient via `jobContact.email` or first `job.jobContacts[].person.email` → "No contact email on file — add a job contact with a valid email."). Computed as a single discriminated union (`{ok:true} | {ok:false, reason}`) so the first missing requirement is what the rep sees — no need to fix everything at once. **Button disabled BEFORE the click** when preconditions aren't met: `disabled={agreementBusy || !portalLinkPrecondition.ok}` + `title={reason}` for the native tooltip + `disabled:cursor-not-allowed` so hover state matches the gate. Rep knows "Needs a booking" / "No contact email on file" without ever firing a 4xx. **Prominent error banner** for failures that DO reach the endpoint (e.g. all preconditions pass client-side but a downstream Resend error surfaces): new `portalLinkError` state distinct from `agreementMsg`. Rendered as a `bg-chip-bad-bg` red-bordered alert with "Portal link not sent" headline + the API's error message + a dismissible × — anchored at the bottom of the agreement section so the rep's eye lands on it post-click. Success path still uses the small green `agreementMsg` strip ("Portal link emailed to X.") — failures get the loud treatment, successes get the quiet one. **Order GET extended (additive)**: `booking.select` now includes `_count: { paperworkRequests: true }` so the precondition can be derived without a separate fetch. Single-line addition; no breaking change to any reader. **Acceptance test against S260609-001** (Jose's test order — BOOKED status, `bookingId: null`, `jobContactId: null`, `company.billingEmail: null`): button now renders disabled with tooltip "Needs a booking before a portal link can be sent." instead of clicking-into-silence. Once a booking + paperwork + contact are wired up downstream, the precondition flips and the button enables — every gate check explicit and visible. `tsc --noEmit` clean.
- `846d41e` — feat(orders): dates optional, no silent 1-day total (1C of 3) — **schema (additive, `prisma db push`)**: `OrderLineItem.billableDays` widened from `Int @default(1)` to `Int? @default(1)`. `prisma migrate diff` previewed a single `ALTER TABLE … ALTER COLUMN "days" DROP NOT NULL` — pure constraint relaxation, zero data rewrites. Existing rows kept their non-null values; the @default(1) stays so any caller that omits the field still gets the legacy 1-day fallback (preserving the pre-1C invariant on every existing read path). Money columns on every other model: untouched. **`computeLineTotal` (src/lib/orders/billing.ts)**: explicit NULL branch — `if (item.billableDays == null) return 0` BEFORE the multiplier logic. Returns 0 firmly rather than silently multiplying by 1. Distinguishing null from 0 matters: 0 is a negotiated freebie the rep set; null is "dates TBD, do not compute." `billingBreakdown` mirrors — undated lines render as `"qty × $rate/day · total on dates"` instead of `"qty × $rate × N days = $total"`. CAP_PER_WEEK + STAGES + EXPENDABLES branches all preserved for dated lines (math is byte-identical when billableDays is non-null). **New-quote page (`new-quote/page.tsx`)**: **today/tomorrow defaults removed** from both blank-row sites — `addBlankItem` (line 967) and the auto-append-after-combobox-commit (line 1106). New blank rows now stamp `pickupDate: ''` / `returnDate: ''` / `billableDays: null` when the editing-level dates aren't set; inherits the editing-level dates when they are. Per-row Days input is null-aware: empty input = TBD (placeholder), typing a number commits a value, clearing reverts to null. Per-line Total cell renders `$X/day` + `total on dates` subtext when billableDays is null and the line isn't EXPENDABLES; otherwise renders the firm total. `ResolvedItem.billableDays` widened to `number | null`. **Send-side (`QuoteDocument.tsx` + `quote-pdf/route.ts`)**: QuoteLineItem interface widened to `billableDays: number | null`. PDF days cell renders `TBD` when null; per-line total cell renders `$X/day` instead of the computed total. **New "DATES TBD — RATE CARD" banner** in the totals block when any non-discount line has null billableDays — surfaces the explicit two-state distinction the spec called for. The line below changes its label from "Subtotal" to "Subtotal (dated lines)" so the client doesn't misread the running total. **Push-dates flow (`datePushPreview.ts`)**: PreviewLineItem.billableDays widened; ProjectedLineItem.billableDaysOld/New widened. Undated lines pass through the push-dates math unchanged — no shift to compute. **Line-items POST (`/api/orders/[id]/line-items`)**: explicit null persistence — when caller sends `billableDays: null` AND no committed dates, persist NULL. Otherwise the existing "inherit from parent / fall back to computeRentalDays" behavior is preserved (matching the pre-1C contract on every other call site that doesn't opt into TBD). **Verified (7-case math fixture)**: dated 3-day GE = $225, dated 5-day STAGES weekly @ 0.90× = $4,500, dated 1-day VEHICLES = $250, EXPENDABLES purchase qty=4 = $48 — all unchanged from pre-1C. Undated GE = $0, undated STAGES = $0, undated qty=3 PRO_SUPPLIES = $0 — never silently 1-day. **Existing orders unchanged**: every persisted OrderLineItem.billableDays was non-null at push time; the math util reads non-null values via the unchanged branch; no historical total moves. `tsc --noEmit` clean.
- `d7bea4c` — feat(orders): Company type-ahead + normalized-key dupe guard (1B of 3) — **shared helper `src/lib/companies/normalize.ts`** exports `companyNameKey()` and `companyNamesMatch()`. Algorithm: lowercase → smart-quote fold → strip non-alphanumeric → trim → tokenize → walk from the end stripping common suffix tokens (`corporation, productions, production, entertainment, pictures, studios, studio, company, films, film, media, group, llc, inc, llp, ltd, corp, co`). Same function used at BOTH the type-ahead match (UI) and the create endpoint (server) — single source of truth, no UI/API drift possible. **No new column needed** — key is computed on the fly; at hundreds of Companies an in-memory pass at write time is fine. **`POST /api/crm/companies` dupe guard**: before insert, fetches all `{id, name, tier, billingEmail}`, walks for a `companyNameKey()` collision, and on hit returns **HTTP 409** with `{error: 'near_match', existing: <row>, normalizedKey, message}`. Caller can override with `allowNearMatch: true` in the body — explicit and audited, never silent. Sanity-tested against 10 fixture pairs: `"Rema Films LLC" ↔ "Rema Films"`, `"Stept Studios" ↔ "Stept"`, `"ACME Pictures Inc." ↔ "acme pictures"`, `"Brix" ↔ "Brix Studio"`, `"Foo Bar Productions" ↔ "Foo Bar Films"`, `"Walt Disney Company" ↔ "Walt Disney Co"` all match. The intentional false-positive `"The Apartment" ↔ "Apartment Productions"` correctly does NOT match — leading-article preservation distinguishes them. **`CompanyPicker` component** (`src/components/shared/CompanyPicker.tsx`) mirrors `JobPicker` exactly: three modes (`searching` / `selected_existing` / `creating_new`), 200ms debounced typeahead against `GET /api/crm/companies?search=`, green-pill / blue-pill confirmed states, "+ Create new company: <typed>" final row. **Recency ranking**: `recentCompanyIds` prop (fed by the recognized lead's `suggestedCompanies` from 1A) pushes those rows to the top of the dropdown — the rep's known companies surface first. **Inline dupe-guard match**: when the typed query normalizes to the same key as an existing hit, that hit gets a `bg-amber-50` highlight + `"matches your typed name"` chip, and the "+ Create new" row's subtitle warns explicitly (`Heads up — "X" already exists with the same normalized name. Pick it above unless this is genuinely different.`). **New-quote page (`new-quote/page.tsx`)**: the `<select>` block + "+ Create new company:" option are gone. CompanyPicker is the input. Sync effect keeps the legacy `selectedClientId` string aligned with the picker mode so the rest of the page (jobs fetch, save path) keeps working unchanged. Lead's suggested companies ALSO render as tap-pills ABOVE the picker for the can't-miss-it variant. Save path now sources the create-name from `companyPick.name` (in `creating_new` mode) and **handles the 409 near-match in-line** via a confirm dialog — `OK = use existing` flips `finalClientId` to the existing row id; `Cancel = create anyway` re-posts with `allowNearMatch: true`. The bypass-create-path concern from STEP 0 is closed: the new-quote page already routed to `/api/crm/companies` (not really a "bypass"), but it was trusting the POST blindly. Now it consumes the 409 response and surfaces the decision to the human. `tsc --noEmit` clean.
- `e4da41d` — feat(orders): person-first recognition on Review Quote (1A of 3) — the AI-extracted contacts payload's three match states are now honored distinctly at the form anchor instead of all rolling into the secondary `PeopleSection`. The dedup work already had `resolvePersonByEmail` (alias-aware) wired into `parse-quote.enrichContacts` — this commit consumes the signal. **New endpoint `GET /api/crm/people/[id]/history`**: live `_count()` over `jobContacts + bookings + referredBookings + orderContacts` (NOT the stale Person.totalBookings/totalSpend/lastBookingAt denorms — booking flow doesn't maintain them), plus the person's recent orders (top 5), affiliations, and a roll-up of suggested companies derived from three signals: recent-order companies (strongest — actual business done), current Affiliation rows, and email-domain hits against Company.website/billingEmail. Suggestions dedupe by id with each entry carrying a `reason` chip ("last order 2026-05-12" / "affiliated · McD World Cup" / "domain match (@rema-films.com)"). **`leadContact` memo** picks the lead from the contacts array in priority order: explicit confirm > `existing` + `existing_person_id` > `possible_match` + `candidate_person_id` > `new`. Status drives three UI strips at the top of Review Quote, distinct treatments per ratification: **`existing`** → green "Recognized" strip with live counts ("N prior orders · last: <project> · most recent: <order# · company>"); when the order has no company yet, the suggested companies surface as a pick list right inside the strip. **`possible_match`** → amber "Is this [name]?" prompt with two buttons — `Confirm — it's them` (sets `confirmedLeadPersonId` which triggers the history fetch + anchor), `Different person — create new` (rewrites the contact row to `match_status: 'new'` so it falls through to the existing `PeopleSection` quick-add). Never auto-anchors — sender could be an assistant emailing on someone's behalf, or a shared address. **`new`** → quiet hint pointing at the contacts panel quick-add; the existing flow is unchanged. **"No client extracted" dead-end replaced**: when `clientCandidates` is empty AND no inquiry company is linked AND the recognized lead has suggested companies, the company field now renders those affiliations as a pick list with reason chips, plus a "+ None of these — create new company" fallback. The old `/crm?selectForQuote=1` round-trip remains as the deeper fallback when the lead has no suggestions either (e.g. a brand-new sender with no domain match in CRM). Schema untouched. No money-math change. Save path untouched — the lead person is still attached via the existing `contacts.find(c => c.match_status === 'existing').existing_person_id` rule. `tsc --noEmit` clean.
- `108133f` — feat(catalog): CSV file-input mode on rate gap-fill — pivot path for when RW's API auth is unusable. New `--csv=<path>` flag on `scripts/rw-rate-gap-fill.ts` reads an RW admin-UI grid export (CSV format) and feeds it through the IDENTICAL matching / preflight / conflict-log / audit / `--apply` pipeline as the API path — same eight buckets, same `RateChangeLog` audit rows, same "conflict never auto-overwrites" rule, same "ambiguous skip" guarantee. Distinct from the prior commit's `--cache=` flag (which replays a prior LIVE RW pull). Lenient header matching: each column name normalized to lowercase + alphanumeric-only, then matched against alias sets so "Daily Rate" / "DailyRate" / "DAILY-RATE" / "Daily Rate ($)" all resolve. **Required columns**: `ICode` (aliases: Item Code, Code, Inventory Code, Asset Code), `Description` (aliases: Item Description, Name, Item Name), `Daily Rate` (aliases: DailyRate, Daily, Rate Daily, Day Rate). **Recommended**: `Weekly Rate` (combobox display consistency — falls back to $0 with a warning if missing). **Optional**: `InventoryId` (aliases: Inventory ID, Master ID, Master Inventory ID) — sets `HQ.rwId` on apply when present; falls back to using `ICode` as the rwId anchor when absent. CSV grid is already at master grain (UI exports one row per master, not per physical unit), so `groupItemsToMasters` is skipped — each CSV row mints one `RwMaster` directly. Money parsing strips `$`, commas, and whitespace; empty or non-numeric values land as $0 (which lands the row in bucket G — RW-also-$0 — not in a bogus fill). Minimal inline CSV parser handles quoted fields with commas, escaped `""` quotes, CRLF/LF endings, optional UTF-8 BOM; no external dep added. Smoke-tested against a 5-row fixture: 3 valid rows + 2 invalid (missing ICode or Description) → 3 masters loaded, 2 dropped at parse time with a warning; the 2 real RW codes (`104484`, `104412`) correctly landed in bucket B (code-exact, safe fill) with their rates surfaced in the preflight markdown. **`--cache=` and `--csv=` are mutually exclusive** — script aborts if both passed. Live-API mode still works and still caches; choose-your-own-input remains the spec.
- `51f78b6` — chore(catalog): cache raw RW pull to disk in rate gap-fill — additive `--cache=<path>` flag on `scripts/rw-rate-gap-fill.ts` plus an unconditional cache-write on every live RW pull (`tmp/rw-items-raw-<ISO timestamp>.json`, with `fetchedAt` envelope). Preflight always caches; `--apply --cache=<path>` skips RW entirely and replays from the cached `RwItem[]`. Why: RW server-side session validity is flapping unpredictably independent of the JWT `exp` claim (verified live — same token bytes returned 200 OK, then 401 six minutes later with nothing changed). Pull-once-work-off-the-cache eliminates the risk of losing the auth window between preflight review and apply. Audit trail unchanged — RateChangeLog still stamps the matched RW master's `rwId` from whichever source supplied it (live or cache). Schema untouched.
- `e40603f` — feat(catalog): RW rate gap-fill primitive + audit table — **schema (additive)**: new `RateChangeLog` model (`inventoryItemId` FK cascade, `oldDailyRate`/`newDailyRate`/`oldWeeklyRate`/`newWeeklyRate` Decimal(10,2), `source` enum `RW_GAP_FILL | MANUAL | ROLLBACK`, `rwIdSource` String? snapshot of the matched RW master, `matchMethod` String? carrying `rwId`/`code`/`name`/`tokens` for the audit trail, `appliedAt`, `appliedById` nullable FK to User). New `RateChangeSource` enum. Two compound indexes — `(inventoryItemId, appliedAt desc)` for "what's this item's rate history?" and `(source, appliedAt desc)` for "what did the last `RW_GAP_FILL` batch touch?". Kept off the generic `AuditLog` model on purpose — money columns deserve a typed shape, not Json blobs. `prisma migrate diff` confirmed a single `CREATE TABLE` + 2 indexes + 2 FKs (additive only, zero modifications to existing tables); `prisma db push` ran clean. **`scripts/rw-rate-gap-fill.ts`** — re-runnable, idempotent, ships ready-to-execute. Pulls RW masters via the existing `fetchAllItems()` + `groupItemsToMasters()` client. Builds four lookup indexes (`rwId`, `ICode`, normalized-name, sorted-token-bag) using the same normalization helpers as `scripts/rw-catalog-import.ts` so the two scripts can't drift their match logic. Walks every active `InventoryItem` and classifies into eight buckets: **(A)** rwId-exact fill, **(B)** code-exact fill, **(C)** name-match fill (REVIEW), **(D)** token-match fill (REVIEW), **(E)** conflict (HQ has rate, RW has different rate — **log-only, NEVER written**), **(F)** ambiguous (>1 RW master matches — **SKIPPED, no guess**), **(G)** HQ $0 + RW $0 (RW master also has no rate), **(H)** HQ $0 + no RW match anywhere. Conflict bucket has **no `--force` flag by design** — a reviewer who decides HQ's stale rate should take RW's value must clear HQ to $0 by hand and re-run; the row then moves to A/B/C. Ambiguous bucket SKIPS rather than guessing; resolution is a manual `rwId` stamp or a more-specific HQ description. Existing-rows-only — no auto-create branch (that's `rw-catalog-import.ts`'s job, deliberately kept separate). `AssetCategory` rates untouched (9 rows, no `rwId` crosswalk, hand-fix is shorter than wiring a fleet matcher). **Fill writes BOTH dailyRate AND weeklyRate** per ratification — combobox display consistency is the rule; a filled daily with $0 weekly would look broken to reps. Weekly retirement is a separate cleanup. On apply, each row is `prisma.$transaction([inventoryItem.update({dailyRate, weeklyRate, rwId (when matched non-rwId), rwLastSyncedAt}), rateChangeLog.create({...})])` — atomic; the audit log can't drift from the data. Operator id stamped from `wes@sirreel.com`'s User row when present. **Preflight mode is the default** (no `--apply` flag): emits `tmp/rw-rate-fill-preflight.md` with the eight bucket tables. Per ratification, the markdown surfaces the two human-judgment buckets prominently — sections **C+D** show every name/token-matched fill with HQ description AND RW description side-by-side so a reviewer can eyeball "is this actually the same item?" (the rwId-exact set is safe to wave through; the ~150-200 name matches are where wrong rates can hide); section **E** shows every conflict with HQ rate, RW rate, AND a Δ column so a reviewer can spot stale-HQ-rate-worth-correcting vs. true RW-side error. STOPS without writes after the markdown lands; re-run with `--apply` for the write pass. **Snapshot invariant verified before build**: traced `OrderLineItem.rate` end-to-end — persisted at create time at `src/app/api/orders/[id]/line-items/route.ts:126`; `recalcOrderTotals` recomputes via `computeLineTotal({rate: li.rate, ...})` reading the stored line rate; `computeLineTotal` in `src/lib/orders/billing.ts` takes rate as a parameter with zero references to `InventoryItem` anywhere in the math path; `discountedTotals.ts` has no `InventoryItem` reference either. Filling catalog rates retroactively **cannot move** any existing order/quote/invoice total. The script's docstring carries the SAFETY CONTRACT explicitly with file paths so future-Wes can re-verify in 30 seconds. **Current state per pre-build probe** (run before the build, against prod): 780 active inventory items, 501 (64.2%) at $0 daily — heaviest pain in G&E (88% of 317 G&E items at $0); 76 rows immediately fillable via rwId-exact; ~425 candidates for name/token matching across the unlinked rows. **Run blocked on token rotation** — local preflight returned `RW GET /api/v1/item → 401 Unauthorized`. `RENTALWORKS_TOKEN` in `.env.local` is stale; rotation runbook at `docs/runbooks/rentalworks-token-rotation.md` (also tracked as an active-roadmap item in `CLAUDE.md`). After rotation, run: `export DATABASE_URL=$(grep DATABASE_URL .env.local | grep -v PRISMA | cut -d'"' -f2) && export RENTALWORKS_TOKEN=$(grep '^RENTALWORKS_TOKEN' .env.local | cut -d'=' -f2-) && npx tsx scripts/rw-rate-gap-fill.ts` — produces the preflight markdown with no DB writes. Apply gate (`--apply`) waits on Wes's explicit OK after reading the C/D/E sections. **Recommendation re-affirmed**: one-time fill, not a cron — RW is being phased out (`CLAUDE.md`), rate-card drift at SirReel is rare (annual-ish), and the conflict-log bucket has no triage owner. Re-runs are idempotent — the `dailyRate=0` filter narrows the candidate set each pass, so any future re-run only touches rows that are still gapped.

## 2026-06-12

- `3476aef` — feat(admin): /admin/dedup review surface — human-in-the-loop merge queue on top of the proven primitive. **Admin gating**: `src/lib/people/dedupAccess.ts` clones the HR allowlist pattern exactly — hardcoded `[wes@sirreel.com, dani@sirreel.com]` + `DEDUP_ALLOWLIST` env override (merge, not replace). `requireDedupAccess()` returns `User | NextResponse` so every endpoint gates with one line. **Schema (additive)**: `Person.dedupSuppressedAt` nullable timestamp. Stamped on every cluster member when reviewer hits "Not a dupe — shared office line"; the cluster builder filters flagged rows so suppressed clusters don't reappear in the default queue. Reversible by clearing the column back to null (no separate endpoint — extremely low frequency). `prisma migrate diff` confirmed a single `ADD COLUMN` (additive). **Cluster builder** (`src/lib/people/buildClusters.ts`): runs the same email-case + phone-digits methods the STEP-0 report used, enriches every member with full FK ref counts (14 tables in parallel-grouped queries), filters @sirreel.com staff (internal, not client relationships), and runs each cluster through `classifyCluster` from the prior commit. Sort: EMAIL-method clusters first (strongest signal — case-only dupes), then by `reviewQueueOrder` (LIKELY_DUPE → UNCERTAIN → OFFICE_MAINLINE; bigger clusters first within each band). Survivor pre-picked per the ratified priority order — refs → portal account → source != null → oldest createdAt. **Merge primitive extension**: `MergePersonsInput` gains `canonicalEmail` (reviewer's pick of which address becomes the survivor's canonical email — the OTHER becomes a PersonEmailAlias unless it normalizes to the same string) and `fieldOverrides` (per-field picks from the conflict UI for firstName, lastName, phone, mobile, role, tier, rawTitle, lastKnownProject, notes). Rejects `canonicalEmail` that doesn't match either input row's email (case-insensitive). Inside the merge transaction, the survivor update merges (a) the canonical email pick, (b) explicit reviewer overrides, (c) null-fill from loser for columns not overridden (enrich-not-overwrite, same rule as the capture pipeline), (d) accumulator sums for Person.totalSpend + totalBookings denorms. **APIs** (all admin-gated): `GET /api/admin/dedup` (cluster queue + counts), `POST .../merge` (calls primitive — returns 409 on conflicts like two-User-row collision; 500 on unknown), `POST .../suppress` (flags every personId in body as `dedupSuppressedAt = now()`), `GET .../recent?days=N` (un-reversed merges in last N days, default 1, with compact loser/survivor summary for the undo button), `POST .../reverse` (calls `reverseMerge` primitive). **`/admin/dedup` page** (`src/app/(dashboard)/admin/dedup/page.tsx`): two-pane layout. Left = cluster cards (LIKELY_DUPE + UNCERTAIN by default; LIKELY_OFFICE_MAINLINE + suppressed hidden behind a single toggle). Per card: header with classification badge + method + key + row count + "Not a dupe (office line)" button; rationale string ("all members share last name 'braverman'; same phone — likely same human"); side-by-side diff table with one column per row in the cluster, one row per field (firstName, lastName, phone, mobile, role, tier, rawTitle, lastKnownProject, notes + email + createdAt + source). Survivor radio in each column header — pre-selected by classifier but reviewer can flip with a click. Field-conflict pickers: every field where ≥2 distinct non-null values exist gets a radio per non-null value; picked value highlights in green; the picks land in `fieldOverrides` on merge submit. Canonical-email selector below the table — `<select>` populated from every row's email; default = survivor's lowercased. Per-loser "Merge X → survivor" button opens a confirm summary INLINE inside the card (refs to repoint, JobContact + Affiliation collision resolution noted, the new canonical email, the alias to be minted, field overrides count, loser archive note); "Confirm merge" calls the API. Right pane = "Recent merges (last 2d)" list with one-click "Reverse" per row — the safety net. Header counts: `N open · M likely dupe · K uncertain · L office line · S suppressed · X merged this session`. **Verified before ship**: every email→Person resolution outside id-based lookups goes through `resolvePersonByEmail` (alias-aware) — `captureFromEmail.ts` (the credibility classifier's existing-contact-match-by-email path, wired in the prior commit), `parse-quote/route.ts` (batch lookup w/ alias fallback), `portal/auth/request/route.ts`, `orders/[id]/portal-access/invite/route.ts`, `orders/[id]/contacts/route.ts`, `bookings/create-send/route.ts`, `crm/captures/[id]/route.ts`, `crm/people/route.ts` POST. The `classifyForCapture.ts` module itself is side-effect-free (verdict + parsed payload only, no Person lookup); its caller does the resolution. Smoke test against prod: cluster builder returns 193 clusters today (185 LIKELY_DUPE with survivors pre-picked, 7 UNCERTAIN, 1 LIKELY_OFFICE_MAINLINE). First three LIKELY_DUPE clusters all show clean signal — Krystin Braverman x4 across job emails, Muji Muhammad x3, Chris Rocco x3 — each with the highest-refs row pre-selected as survivor. **Not in this ship**: bulk-merge cron (intentional per ratification — human-in-the-loop only); /admin/dedup nav link (Wes can pin it directly).
- `4d8f7c4` — feat(people): Person dedup primitive — audited, reversible, alias-aware, canary-verified. **Root-cause fix (write paths)**: `src/lib/people/email.ts` exports `normalizeEmail()` (trim + lowercase) + `resolvePersonByEmail()` (Person.email case-insensitive + PersonEmailAlias case-insensitive). Every Person email write path now normalizes — `/api/crm/people` POST + PUT, `/api/portal/authorize/[token]` magic-link flow (the original site of the Wes case-split), `/api/orders/[id]/portal-access/invite`, `/api/orders/[id]/contacts`, `/api/bookings/create-send`, `/api/crm/captures/[id]` add path, `src/lib/crm/captureFromEmail.ts`. Every email→Person lookup goes through `resolvePersonByEmail` so a merged loser's address still resolves to the survivor instead of re-minting the dupe — alias-aware on the magic-link auth request, the capture pipeline's existing-contact match, the new-quote parse-quote enrichment (batch variant scans `PersonEmailAlias` for misses), and the inline add-contact flow. **Schema (additive)**: new `PersonEmailAlias` (email @unique → personId FK with cascade-delete, optional `mergeId` FK for reversal cleanup), new `PersonMerge` (survivor FK, `survivorEmailBeforeMerge` for canary-case reversal, `loserSnapshot` Json, `repointLog` Json, `aliasIds` String[], merged/reversedAt + by FKs, `reversalErrors` Json for failure triage). Both built additively via `prisma migrate diff` → `prisma db push` (CREATE TABLE + indexes only, zero modifications to existing tables). **Backfill**: `scripts/lowercasePersonEmails.ts` walks every Person row, lowercases mixed-case emails when no collision exists, surfaces collisions as "use merge UI" (the Wes case). Applied in prod — 1 row lowercased (`ESwanstrom@fabletics.com` → `eswanstrom`), 1 collision deferred to canary (the Wes pair). **Stray-ref grep**: scanned 23 JSON/JSONB columns + 56 TEXT columns (notes/description/body/subject) for both canary Person ids — zero hits outside the formal FK graph. The 14-FK list (Booking.personId, Booking.referredById, JobContact.personId, Order.jobContactId, Affiliation.personId, OutreachActivity.personId, Activity.personId, EmailMessage.personId, Inquiry.personId, InquiryCapture.personId, PersonSession.personId, PortalAccess.contactId, User.personId, Person.worksWithId) is complete; no audit-log or jsonb embedded ids to worry about. **Merge primitive (`src/lib/people/mergePersons.ts`)**: single-transaction repoint of all 14 FKs. JobContact handles `@@unique([jobId, personId, role])` collision by deleting the loser's redundant row + logging the snapshot for reversal restore. Affiliation handles `@@unique([personId, companyId, productionName])` by summing totalSpend + totalBookings into the survivor's row and logging both the delta (for reversal subtract) and the loser row snapshot (for reversal restore). User.personId @unique collision — both loser AND survivor have a User row — ABORTS the merge with an explicit message: two portal accounts for one human needs human triage, not silent merge. Survivor email gets normalized to lowercase AFTER the loser row is deleted (order matters — otherwise the canary case collides on @unique). PersonMerge audit row committed INSIDE the transaction, never after, so the reversal log is always complete on success. Alias inserted iff loser email lowercased != survivor email lowercased (skip the redundant row in the canary case). **Reverse primitive (`src/lib/people/reverseMerge.ts`)**: walks `repointLog` in reverse — restores survivor's pre-merge-case email (if captured) FIRST, recreates the loser row from snapshot with its original id (external caches keep working), then reverses each entry: `repoint` moves rows back, `delete` recreates with snapshotted columns, `sum` decrements the survivor's accumulator by the captured delta. Drops PersonEmailAlias rows minted by this merge. Stamps `reversedAt + reversedById`. Reversal failures land on `PersonMerge.reversalErrors` (the transaction rolls back; the row stays unreversed; human is paged). Same `reversedAt` guard prevents double-reverse. **Cluster classifier (`src/lib/people/clusters.ts`)**: pre-sorts the 195 phone clusters from STEP 0. Same-phone + 1 distinct normalized last name → `LIKELY_DUPE` with pre-selected survivor (per the ratified priority: refs → portal account → source != null → oldest createdAt). Same-phone + ≥3 distinct last names → `LIKELY_OFFICE_MAINLINE` (do not merge — that's a reception line). 2 distinct last names tiebroken by first-name alignment (covers maiden/married surname variants). `reviewQueueOrder` puts LIKELY_DUPE first, UNCERTAIN middle, OFFICE_MAINLINE last — reviewer attacks high-confidence + high-leverage first. **CANARY (`scripts/canaryMergeWes.ts`)** — round-trip verified end-to-end: merged `04a27d0d…` ← `bc19098c…` (Wes pair), survivor refs 14+4=18 confirmed, email lowercased to `wes@sirreel.com`, alias correctly skipped (emails normalized to same value); REVERSED — loser restored with original id, both rows' ref counts back to pre-merge values, PersonMerge.reversedAt stamped; RE-MERGED — clean state, refs back to 18. Three states, three transactions, all auditable. **What's NOT in this shipment**: no batch-merge cron (per ratification), no admin-gated `/admin/dedup` UI yet (canary stands as the proof-of-primitive; UI follows in the next commit). Known v1 boundary acknowledged: dupes with name variations + no shared phone + different emails won't be caught by any of the three methods — that's fine, the alias model + normalized writes prevent future dupe creation. Production state after this ship: 4,633 people (1 mixed-case email cleaned by backfill, 1 dupe cluster merged by canary, net 4,632 — Wes the canary is now a single row with consolidated history).
- `c4b8ef3` — fix(orders): pin flat-total order discount live — order-scope flat-total discounts no longer freeze a dollar amount at save time and drift when line items / dates change. **Schema (additive)**: new `FLAT_TOTAL` value on `DiscountType` enum (`prisma migrate diff` previewed a single `ALTER TYPE … ADD VALUE` — no DROPs, no rewrites of existing rows; `prisma db push` clean). For `FLAT_TOTAL` rows, `OrderDiscount.value` stores the TARGET GRAND TOTAL the user typed (e.g. `2500.00`), NOT a derived discount dollar. `PERCENT` and `FIXED` unchanged. **Math (`src/lib/orders/discountedTotals.ts`)**: order-discount branch grew an explicit `FLAT_TOTAL` arm that derives the applied discount every render: `orderDiscount = clamp(discountedSubtotal − target/(1+taxRate), 0, discountedSubtotal)` — inverse of the post-discount math so the order total lands EXACTLY on the target. **Margin clamp (a single clearly-commented conditional so policy is trivial to flip later)**: if the implied discount goes negative — subtotal dropped below the flat target — clamp to `$0` and emit `flatTotalClamped: true` on the returned breakdown. Never silently mark an order up through the discount field. Also added `flatTotalTarget` to the breakdown so the UI knows what the user originally entered. **API (`/api/orders/[id]/discounts` POST/PATCH)**: `VALID_TYPES` now includes `FLAT_TOTAL`; POST rejects `FLAT_TOTAL` for `scope=DEPARTMENT` (department subtotals are pre-tax, the conversion is exact, no drift risk — dept flat-total keeps funneling into `FIXED` at the form layer); PATCH rejects type-flip to `FLAT_TOTAL` on an existing `DEPARTMENT` row (scope is immutable, same constraint). **UI (`DiscountsPanel`)**: order-discount `Flat total` mode now submits `{type: 'FLAT_TOTAL', value: targetTotal}` instead of the prior derived-FIXED conversion. Existing-row display shows `−$X (→ $Y total)` so the rep can see both the live-derived discount AND the pinned target. Preview text under the form swapped from "Will store as a FIXED discount … later edits will move the total visibly" to "Live-pinned to the target — adding/removing line items or changing dates will move the discount automatically so the total stays at $X." Clamp warning row renders in amber under the order-discount block when `breakdown.flatTotalClamped` is true: `"Subtotal ($X) is below the flat target ($Y) — discount clamped to $0."` **Invoice generator (`generateRentalInvoice.ts`)**: the standalone order-discount line computation now handles `FLAT_TOTAL` the same way (target → pre-tax-from-target → discount), so invoice PDF math matches the order detail UI. **Locked-order guard (`recalcOrderTotals`)**: bails BEFORE any write when `order.status` is `INVOICED` or `CLOSED` — returns the persisted `subtotal/taxAmount/total` unchanged so the new live-derivation can't shift a paper-trail-committed total as a deploy-time side effect. Open orders (`DRAFT` through `RETURNED`/`LD_CHECK`) recompute normally. **Legacy data migration**: one-shot `scripts/migrateLegacyFlatTotal.ts` (idempotent — filters `type: 'FIXED'`, already-converted rows won't re-match). Parses target from auto-generated labels of the form `"Flat $X[,XXX][.YY] total"` via regex; for each matched row, swaps `type → FLAT_TOTAL` and `value → parsed target`, then re-runs `recalcOrderTotals` so the order's persisted total snaps back to the target. **Skips DEPT-scope rows AND locked orders** (`INVOICED`/`CLOSED`). Applied in prod: 2 candidate rows; 1 converted (`S260609-001`, BOOKED — the exact order from the bug report, was drifting to $2325, now pinned at $2500), 1 skipped (`S260610-001`, CLOSED, total stays $1000 untouched). Verification: simulated FLAT_TOTAL math across 5 scenarios — initial subtotal, subtotal-down, subtotal-up, subtotal-below-target (clamp), and tax-bearing — total locks to the target in every non-clamp case; clamp case correctly returns `total === subtotal` with `flatTotalClamped: true`. Existing PERCENT and FIXED order discounts go through the exact same branches as before; the `else` path is byte-identical. `zeroDiscountSanityHolds` self-check still passes (no discount rows → `flatTotalClamped: false`, all other fields unchanged).
- `9e652db` — fix(orders): rebalance line-item table columns — new-quote `TABLE_GRID` updated from `'grid-cols-[64px_minmax(220px,1fr)_96px_136px_136px_120px_104px_36px]'` to `'grid-cols-[64px_minmax(280px,1fr)_90px_140px_140px_72px_90px_36px]'`. DESCRIPTION absorbs available width (minmax bumped 220→280 + still `1fr`) so long product names breathe instead of fighting the dept badge + status chip for inline space; QTY stays 64; PRICE/DAY tightened 96→90 (the `$` adornment + 2-decimal display fits comfortably at 90); PICKUP/RETURN bumped 136→140 for date inputs; **DAYS** column shrunk 120→72 and the header label renamed `Billable days` → `Days` (centered) so the column label no longer drives its own width; TOTAL tightened 104→90 (right-aligned amounts at 2 decimals fit). The days `<input>` got `text-center` + `px-1` so the value sits centered under the now-narrow header. Header row, item rows, and subtotal row all read the same `TABLE_GRID` constant so columns align vertically across groups — single source of truth, no drift. EXPENDABLES still suppress PICKUP/RETURN/DAYS labels + render em-dashes in those cells; group "Apply to category" header strip is `flex-wrap` so it wraps cleanly on narrower viewports rather than blowing out the row. No schema, no math, no persistence changes — pure display layer.
- `8d8e7ad` — chore(email): verify DKIM signing domain empirically — sent a real probe through the prod Resend path to `wes@sirreel.com`, then DWD-impersonated and read the delivered headers. `DKIM-Signature: ... s=resend; d=sirreel.com` — root-domain signed. `Authentication-Results: dkim=pass header.i=@sirreel.com; dmarc=pass header.from=sirreel.com` — Gmail confirms. Return-Path is `send.sirreel.com` for bounce routing (normal Resend infra; **NOT** the signing domain). Conclusion: the long-standing iCloud subdomain-DKIM risk does NOT apply; the CRM "MAY BE FILTERED" iCloud badge is either precautionary or about a different iCloud quirk. New `scripts/verifyDkimDomain.ts` is the reusable check — re-run any time Resend domain config changes.
- `3f82c57` — feat(orders): USD currency formatting on price inputs + totals — shared `<CurrencyInput>` component (`src/components/ui/CurrencyInput.tsx`) renders a muted `$` prefix adornment inside the input padding; display formats to two decimals on blur (`3` → `3.00`, `3.6` → `3.60`), flips to a raw editable string + `select()` on focus so cursor isn't fighting the prefix, lenient parse strips `$` / commas / whitespace, Enter blurs. Optional `min` / `max` clamps on blur — line-item rate fields explicitly pass `min={0}`, the discount Amount field omits min so negatives still work. Schema and math untouched — `onChange` emits a plain `number`. **Swapped in everywhere**: new-quote `PRICE/DAY` column (was raw `step="0.50"` number input), new-quote Discount Amount, order-detail modal Rate input, order-detail inline rate-edit input (now with proper select-all + `/day` suffix preserved), order-detail Record-Payment Amount input, admin Package Builder price/day. **Totals + subtotal formatting**: the new-quote `fmtMoney` formatter had `maximumFractionDigits: 0` — that's why subtotals were rendering as `$7` and totals as `$3` in the screenshot. Updated to `minimumFractionDigits: 2, maximumFractionDigits: 2` so the whole table reads `$3.00 / $4.00 / $7.00`. The order-detail page's `fmt`, the admin-packages `fmtUsd`, and the combobox dropdown's `FORMAT_USD` all already used Intl default (2 decimals for USD); no change needed.
- `6921cf1` — feat(orders): Packages — catalog builder + order integration — curated bundles of inventory items sold at a single per-day price. **Schema (additive)**: new `Package` (id cuid, name, description, department LineItemDepartment, pricePerDay Decimal, active, timestamps) and `PackageItem` (packageId FK Cascade, inventoryItemId FK Restrict, qty default 1). `OrderLineItem` gains `packageInstanceId String?` (groups header + members per package instance — cuid minted at add-time), `isPackageHeader Boolean`, `isPackageModified Boolean`, `packageId String?` FK (`onDelete: SetNull` so deleting a Package template keeps historical orders intact). `prisma db push` ran clean, additive only. **Admin builder** at `/admin/packages`: left-rail list with active-toggle pill + selected-row highlight; right-pane edit form with name, dept select, description, price/day, and an item picker using the **same** `LineItemDescriptionCombobox` in `types={['INVENTORY']}` mode. Each added item shows code, standard daily rate, qty stepper (− / number input / +), running line total. Running "Component value: $X/day" panel sits next to the price input with the implied discount % ("Package saves 22% vs à la carte" / "Package is 5% MORE than à la carte"). Items add by combobox pick (dupes bump qty); list rows show standard rate; delete via X button. Soft-deactivate via the active toggle hides from the picker without losing historical references. **Catalog search**: `/api/catalog/search` now returns Package hits alongside Inventory + AssetCategory. New `?types=` query (comma-separated INVENTORY/ASSET_CATEGORY/PACKAGE) lets callers scope — the line-item combobox uses default (all three), the package builder's picker uses `INVENTORY`. Package hits include the `items[]` array fully expanded with inventoryItemId, name, qty, dailyRate, weeklyRate, department so the picker can mint member rows client-side without a follow-up fetch. Combobox dropdown renders a violet `PKG` badge + member-count subtitle to distinguish from items + categories. **New-quote integration**: picking a PACKAGE escapes the per-row patch flow via a new parent-level `handlePickPackage` — replaces the target row with a header (description=pkg.name, rate=pkg.pricePerDay, qty=1, `isPackageHeader=true`, fresh `packageInstanceId` from `crypto.randomUUID()`) and inserts N member rows after it (rate=0, qty from template, shared packageInstanceId, dates inherited from header). Members render with a violet `↳ member` chip + `bg-violet-50/30 pl-8` indent. Header row shows a `PKG · header` chip and, when modified, an amber dot tooltip "contents modified — verify pricing". **Behavior rules**: (a) header date/billable-day changes cascade to members via a `headerCascadeFields` branch in `updateItem` — availability depends on member dates; (b) deleting a header opens a confirm dialog ("Remove entire package?") and removes all members in the same packageInstance, with full-bundle undo via the existing toast; (c) deleting OR editing qty/description/rate on a member sets `isPackageModified` on the header so the rep sees the amber warning before sending; (d) member lines are excluded from the empty-row prune (they're $0 by design, not empty workspace); (e) totals math unchanged — header carries the price, members are $0. **Save path**: line-items POST API gained `packageInstanceId`, `packageId`, `isPackageHeader`, `isPackageModified` fields; new-quote sends them on every row so package grouping survives a page reload. **Order-detail modal**: picking a PACKAGE bypasses the modal entirely, fires `POST /api/orders/[id]/line-items/from-package` (single transaction creates header + members with shared instanceId), refreshes the order, closes the form. **Invoice PDF**: `InvoiceLineSnapshotEntry` gains `isPackageHeader?` + `isPackageMember?` flags; `InvoiceDocument.tsx` renders members indented with `· ` prefix, gray italic "included" in the Rate column and "—" in Amount; headers render normally with their price. `generateRentalInvoice.ts` maps the flags through from each OrderLineItem. **OrderDiscount system untouched** — package pricing is independent; order- and department-level discounts apply on top exactly as before. Pick list / inventory views are unaffected (no filter excludes $0 lines today).
- `1989b2f` — fix(orders): line-item combobox stays open after pick + truncates long names — two bugs in the inventory combobox shipped at `7ad90f2`. **Bug 1: dropdown didn't dismiss after selection.** Root cause: in-flight fetch responses could re-open the dropdown a frame after `pick()` because the cancel flag (set during the effect's cleanup) might lose to the response handler's setOpen(true) in some render orderings. Added a `justPickedRef` belt-and-suspenders alongside the existing `dismissed` state — ref blocks the response handler from re-opening within the same paint, state blocks the debounce-fetch effect on subsequent renders. `pick()` now slams the dropdown shut (`setOpen(false)` + `setResults([])` + `setDismissed(true)` + `justPickedRef.current = true`) BEFORE the parent state updates propagate, so any racing fetch sees the lock. `handleFocus` no longer resets `dismissed` — keystrokes are the only path back into the open state. Any keystroke clears `justPickedRef` + `dismissed` so the typeahead resumes normally. **Bug 2: long item names ellipsized.** Dropdown was sized to `left-0 right-0` (matching the narrow description column), and each row used `truncate` (`overflow:hidden; text-overflow:ellipsis; white-space:nowrap`) — "Caravan Canopy 10x10 EZ-Up — Black" rendered as "Caravan Cano...". Fixed: dropdown now `style={{ minWidth: '100%', width: 'max-content', maxWidth: '480px' }}` so it sizes to its longest hit (capped at 480px); row name swapped to `whitespace-normal break-words` so long names wrap onto two lines; price-per-day stays right-aligned, `pt-0.5` keeps it baseline-aligned when the name wraps; row container now `items-start` for the same reason. Both fixes apply everywhere the combobox is used (new-quote inline rows + order-detail modal).
- `7ad90f2` — feat(orders): inline inventory combobox on line-item description — kills the separate "Pick one" search step. **(1) Token-based matching** on `/api/inventory/search` AND `/api/catalog/search`: query splits on whitespace and AND-matches every token against `code OR description OR aliases` (case-insensitive contains). `"6' Table"` finds `"6' Folding Table"`, `"sandbag"` finds `"25 LB. SANDBAG"`, `"25 sandbag"` correctly narrows — verified live. Single-token queries collapse to the original OR clause; zero regression on existing call sites. **(2) Shared `<LineItemDescriptionCombobox>` component** (`src/components/orders/LineItemDescriptionCombobox.tsx`): one input, dropdown under the field showing `name · department · daily rate`, debounced 200ms against `/api/catalog/search`. Keyboard: ↑/↓ navigates hits, Enter on highlighted = pick + commit, Enter with no highlight = commit row, Esc dismisses dropdown (keeps free text), Tab moves on. Quiet `custom` chip replaces the prior red `⚠ No catalog match` warning — custom is a valid choice, not an error. `✓ item` / `✓ category` chip when bound. Text edits don't auto-unbind unless the rep deletes the description entirely; picking a different hit explicitly replaces the binding. `forwardRef` to the underlying input for parent focus management. **(3) New-quote wiring**: row's description input + the "Pick one" override panel + "No catalog match" / "Matched: X" chips all removed. The combobox replaces them. **AUTO-APPEND ROW** on commit: parent watches `onCommit(idx)`, finds the next existing row in the SAME department (focuses it) or appends a fresh blank row in the same dept and focuses its description (`requestAnimationFrame` + `setSelectionRange` for clean cursor placement). The rep runs down a phone-inquiry list keyboard-only — description → qty → next row. **EMPTY-ROW PRUNE** at `createQuote`: discards rows where `description.trim() === '' && catalogProductId == null`. By emptiness, not position — a blank mid-list gets cleaned too. Trailing empty rows never reach PDF / totals / DB. **(4) Order-detail modal wiring**: the EQUIPMENT/EXPENDABLE `invSearch` input + manual dropdown are replaced by the same combobox. Picking an `ASSET_CATEGORY` hit auto-flips `liType` to VEHICLE and binds via the existing `selectAssetCategory`; picking an INVENTORY hit calls the existing `selectInventoryItem` so rate auto-fill + description auto-fill behave identically to today. `hideCustomChip` keeps the modal's status pills tidy. VEHICLE and LABOR/FEE paths unchanged.
- `fd2055c` — feat(orders): one-flow quote build + TSX send with inline client create — three pieces stitched into one continuous motion: sales builds a quote and sends the TSX welcome+quote email in a single sitting, including phone-inquiry clients who aren't in CRM yet. **(1) TSX welcome template** (`src/lib/email/templates/tsxWelcomeTemplate.ts`): two-mode (`welcome-only` / `welcome-with-quote`), same visual shell as the thank-you (slate header, Bradley Hand "Welcome!" badge at -20° tilt, gold-rule-framed `T S X – T H E  S I R R E E L  E X P E R I E N C E` tagline above the footer, rep + "& Team SirReel" sign-off). Quote-mode includes a snapshot block (order#, dates, subtotal/total) with a big amber **"View quote & portal →"** CTA pointing at the magic-link portal URL on tsx.sirreel.com — and explicitly NOT a PDF attachment. **PDF download lives inside the portal** (`/portal/job/[slug]` already exposes "Download quote PDF" via the existing PaperworkRow); smaller email, better deliverability, less intimidating first touch. `[[PLACEHOLDER]]` markers in the template file flag every spot of starter prose for Wes review before first real send. **(2) Inline client create on `/orders/new-quote`**: the AI-extracted contacts section now ALWAYS renders (was hidden when empty) with a "**+ Quick add contact**" affordance — appends a blank row with `include=true` and `match_status='new'` that the rep fills in (name, email, phone, role). Save handler stamps `source="phone_inquiry"` on the minted Person row when the row carries no email-parse provenance (rep typed it cold) — distinguishes phone leads from email-derived contacts for the CRM stats strip and capture review. `POST /api/crm/people` gained a `source` body field (free-form, defaults null, trimmed). **(3) Send-as-finishing-move CTA**: new "**Send quote →**" amber button next to the existing Save Draft / Preview / Download row. Same create path as draft, then navigates to `/orders/[id]?send=1` instead of `/orders/[id]`. The detail page's `useEffect` watches the query, auto-opens the existing `EmailReviewModal` against the TSX welcome+quote template, and scrubs the param so a hard refresh doesn't re-open. Save Draft remains the secondary path; nothing about the existing draft flow changes. `composeQuoteEmail` rewired to call `buildTsxWelcomeEmail` in welcome-with-quote mode, with the quote snapshot fields (start/end/subtotal/total) and `agent.phone` added to the order select. PDF attachment removed from both preview meta + send dispatch; `getBlob` import dropped from the send route since the buffer fetch is no longer needed.
- `ca91cb1` — feat(portal): serve client portal at tsx.sirreel.com — host-based routing inside the same Vercel project. New `middleware.ts` at project root: requests to `tsx.sirreel.com` allow-list portal + public-client paths (`/portal/`, `/api/portal/`, `/client/`, `/api/client/`, `/intake/`, `/api/intake/`, `/api/public/`, `/order/supplies`, plus Next/Vercel internals + logo assets) and **404 everything else** — a client can never see the staff dashboard on tsx. Root path `/` → 307 to `/portal/auth/sign-in`. Requests to `hq.sirreel.com` for any of those client paths → **308 permanent redirect to the tsx equivalent** (path + query preserved; old email links keep working forever). Local dev + Vercel previews pass through unchanged. New centralized URL helper at `src/lib/portal/portalUrl.ts` (`portalBaseUrl` / `portalJobUrl` / `portalTokenUrl` / `portalAuthorizeUrl` / `portalSignInUrl` / `portalAuthVerifyUrl` / `portalResendLandingUrl` / `clientTokenUrl`) reads `PORTAL_BASE_URL` env (set in Vercel prod to `https://tsx.sirreel.com`) with a hardcoded prod default. Converted every hard-coded `https://hq.sirreel.com/portal/...` site to the helper — 16 files: invoice/sendInvoice, cadence/context, send-quote/follow-up/agreement/contract-review-accept/portal-access (+invite, authorize-ask), portal/authorize/[token], portal/job/[slug]/resend-link, portal/job/data, bookings/send-portal + create-send, orders/[id]/agreement + resend-link, contacts, and the portal share-link mailto on `/portal/[token]` (now uses `window.location.origin` so the share-link is host-correct regardless of which tsx/hq landed). quoteSend.ts + followUpSend.ts asset HOST now reads `PORTAL_BASE_URL` so the SirReel logo + the supply-order CTA in those emails point at tsx. **Cookies stay host-only** (no `domain` attribute) — the `sr_portal_session` and `sr_person_session` cookies are set BY the verify routes which now serve on tsx, so they naturally scope to tsx with no code change. Clients re-auth once via magic-link email during cutover; acceptable per STEP 0 brief. Staff session (`getServerSession`) is touched only by `/api/orders/[id]/portal-access/invite` which lives on hq — correct, that's staff inviting a client; the email it sends contains the new tsx URL.
- `f524049` — feat(orders): caption-optional + agent weekly candid workflow — two changes folded together. **(1) Photo caption default-empty**: removed the hard-coded "A moment from your shoot." line from the template; caption renders only when the rep types one in on the compose page. New `photoCaption` field threaded through `buildThankYouEmail` → preview → send. **(2) Agent weekly candid**: additive `AgentWeeklyCandid` model (`userId`, `fileUrl`, `blobKey`, `mimeType`, `sizeBytes`, `weekStartDate`, `capturedAt`). Storage path `agents/<userId>/<yyyy>/<mm>/candid-<uuid>` — uses the same `'private' as 'public'` cast as the claims helper (the private-blob-store gap from STEP 0 still applies; public-store fix is owed for the URLs to embed in real recipient mail). Monday-of-week computed in Pacific via `src/lib/orders/weekStart.ts` so a Monday-morning upload and a Friday-evening upload map to the same week-keyed row. New endpoints `POST/GET /api/users/me/weekly-candid`. New mobile-friendly `WeeklyCandidWidget` on Sales + Admin dashboards: prompts when no candid is set OR last one is from a previous week (amber); "Set for this week" (emerald) when fresh. Guidance copy: "Aim for the SirReel sign in frame · warehouse crew, a fleet vehicle, or a piece of gear in the background · candid feels better than posed." Camera input `<input type="file" accept="image/*" capture="environment">` opens the rear camera directly on iOS / Android. **Compose-page photo picker overhaul**: three radio modes — `My weekly candid` (default when present, shows thumbnail + freshness), `A photo from this order` (existing JOB_PHOTO grid — warehouse team's per-order uploads, collapsed behind a `<details>` toggle), `No photo`. Above the radio list: a primary **+ Take a new candid right now** button that uploads to the weekly-candid slot AND auto-switches the source — exactly what the spec asks for ("option to take a new photo right at that moment or to go with the photo they uploaded at the beginning of the week"). New `photoUrlOverride` field on preview + send routes routes the weekly candid's URL into the template without going through `OrderDocument`. Caption input lives in the same section, label clarifies "Leave blank — most candids speak for themselves."
- `64f4654` — feat(orders): branded thank-you compose with candid photo — compose view at `/orders/[id]/thank-you`. **Template** at `src/lib/email/templates/thankYouTemplate.ts`: branded shell (SirReel logo, slate header `#0f172a`, gold accent `#D4A547`, warm body) with merge fields (clientFirstName, jobName, orderNumber, wrapDate, agentName + displayTitle, agentPhone, optional photoUrl, optional personalNote line above the body). **⚠ Subject + body PROSE are PLACEHOLDER COPY — clearly flagged in the file with `[[PLACEHOLDER]]` comments AND a yellow banner at the top of the compose page** so the team can't miss it. Wes review owed before first real send. Photo embedded as `<img src>` pointing at the hosted Vercel Blob URL (NOT base64 — Gmail blocks `data:` URIs and a large share of clients would render a broken box). **Upload pipeline**: new `src/lib/orders/uploadOrderDocument.ts` mirrors the claims helper but writes to `orders/{yyyy}/{mm}/...` with Vercel Blob `access: 'public'` so the email's `<img>` resolves. `POST /api/orders/[id]/documents` accepts multipart with mobile camera capture (`<input type="file" accept="image/*" capture="environment">` — warehouse team shoots on phones) AND desktop file picker (drag-drop button), stamps `OrderDocument` with `uploadedById` audit. **Send path**: `POST .../thank-you/preview` and `POST .../thank-you/send` are the standard preview/send pair (matches the quote-send pattern). `sendAgreementEmail` got a minimal additive `replyTo` field on `EmailPayload`; thank-you send uses it to set reply-to = `agent.email` so client replies land in `jose@/oliver@/etc.` where the watched-inbox pipeline and CRM capture already process them (a routing alias would take replies OUT of that system). On send: `ThankYouSuggestion` SUGGESTED → SENT with sentAt/sentToEmail/sentById/photoDocumentId/personalNote, plus an `AuditLog` row `action: order.thank_you_sent`. `POST .../thank-you/dismiss` is the muted lane with an optional free-text reason. Compose page is two-column on desktop (compose left, sandboxed iframe preview right), stacks on mobile.
- `a9f5314` — feat(orders): thank-you suggestion queue — trigger + dashboard widget. **Trigger**: `PUT /api/orders/[id]` now `prisma.thankYouSuggestion.upsert({ where: { orderId } })` whenever a status transition lands on `RETURNED` (idempotent via the `@unique orderId` FK — a RETURNED → not-RETURNED → RETURNED bounce never double-mints). Never auto-sends; the human reviews and sends. **Queue endpoint** `GET /api/orders/thank-yous` returns SUGGESTED rows with surrounding triage context: client, job, agent, wrap date + days, age days, photo status (any `OrderDocument.JOB_PHOTO` on the order OR a pinned `photoDocumentId`), and warn flags — `openIncident` (any `Incident.orderId = this AND status NOT IN (RESOLVED, WRITTEN_OFF)`) and `unresolvedLd` (open Incident with damage items, the cheap proxy for "L&D not yet billed"). `?scope=mine` filters to suggestions on orders where the session user is the agent. **Widget** `ThankYousToSendWidget` (mobile-friendly, `bg-white` card matching the existing dashboard widgets): collapsible header with counts ("N jobs · K expired"), per-row chips for `openIncident` (red), `unresolvedLd` (amber when no incident), and `expired` (>14 days — visual only, still sendable). Mounted on `SalesDashboard` (scope=mine, below My Pipeline — Jose/Oliver see their own pile) and on the admin `Dashboard` (scope=all, between CollectionsReportWidget and ReviewsWidget — Wes backstops). Each row links to `/orders/[id]/thank-you` (STEP 3 compose view).
- `8d8e7ad` — feat(orders): thank-you suggestion schema — additive groundwork for the post-job thank-you flow. New `OrderDocument` model with `OrderDocType` enum (minimal: `JOB_PHOTO | OTHER` — types added on real use, never speculatively); mirrors `ClaimDocument` / `HrAttachment` patterns. `fileUrl` is the hosted Blob URL (Gmail blocks `data:` URIs; hosted is the only reliable inline-image path). New `ThankYouSuggestion` model: `orderId @unique` (idempotent on RETURNED bounces), `status: SUGGESTED | SENT | DISMISSED`, `photoDocumentId?` FK to `OrderDocument`, optional `personalNote`, audit fields (`sentAt / sentToEmail / sentById / dismissedAt / dismissedReason`). Visual 14-day expiry is computed off `createdAt` at render time — no cron. `prisma db push` ran clean (additive only, zero DROPs).
- `5499ac8` — feat(crm): outreach timeline + follow-ups due wiring — three pieces. **(1) Timeline merge**: `/api/crm/people/[id]` and `/api/crm/companies/[id]` now return `outreachActivities`; person + company detail pages weave them into the existing timeline (activities + outbound emails) with a green `OUTREACH · TYPE` chip, who-logged-it, occurredAt, and a follow-up date pill when present. **(2) FOLLOW-UPS DUE merge**: `/api/crm/stats` `followUpDueCount` now sums BOTH `Activity` (where `completed=false AND dueDate <= now`) AND `OutreachActivity` (where `followUpDone=false AND followUpAt <= now`) — one unified number so the strip card is the source of truth regardless of which surface logged the follow-up. New `GET /api/crm/follow-ups` endpoint returns the merged list with `kind: 'activity' | 'outreach'`, person/company links, type chips, due-date overdue math. Clicking the FOLLOW-UPS DUE card on `/crm` now opens a drill-down modal (`FollowUpsDueModal`, mobile-first sheet on phone, `md:max-w-2xl` on desktop) listing every due item with one-tap **Done** (`PATCH /api/crm/outreach/[id] {followUpDone:true}` or `PATCH /api/crm/activities/[id] {completed:true}` depending on `kind`) and one-tap **+ Log outreach** (opens the quick-log modal pre-linked to that row's person/company). **(3) "My outreach" filter**: `?scope=mine` on `/api/crm/follow-ups` limits to follow-ups created by the session user (`agentId === user.id` for legacy Activity, `createdById === user.id` for OutreachActivity). The drill-down modal exposes this as a checkbox at the top — sales reps see their own pile; ADMIN / MANAGER can clear the box and see everyone's.
- `c9aa32d` — feat(crm): mobile-first outreach quick-log — Oliver's in-the-field flow. Four taps + notes = saved. New `OutreachQuickLogModal` with type chips (`VISIT / CALL / EMAIL / TEXT / EVENT / DROP_IN` — 6 large tap targets in a 3-col grid, ≥48px tall), person typeahead reusing existing `/api/persons?q=` endpoint, company typeahead reusing `/api/crm/companies?search=`, **inline quick-add** ("+ Quick add &lt;query&gt;" appears when the typeahead returns zero hits — Oliver meets people not in CRM yet, types the name, taps quick-add, fills first/last/email, POSTs `/api/crm/people`, and lands back in the outreach modal with the new person pre-selected), notes textarea, follow-up presets (`Tomorrow / 3 days / 1 week / 2 weeks` — one-tap) plus a manual date picker. Layout is full-screen sheet below `md:` and a centered modal `md:max-w-lg` on desktop — Oliver's phone gets every pixel; the same component on the desktop CRM page gets a normal modal. New routes `POST /api/crm/outreach` (creates the row, app-layer at-least-one-of-personId-or-companyId check, stamps `createdById` from session) and `GET /api/crm/outreach` (filters by personId / companyId / createdById / pendingFollowUpsOnly — STEP 3 wires this). New `PATCH/DELETE /api/crm/outreach/[id]` for follow-up `Done` toggle + edits; authorship gate (rep edits their own; ADMIN/MANAGER edit anyone's). "+ Log outreach" trigger lives on the `/crm` header (`min-h-[2.5rem]`, amber CTA), on `/crm/people/[id]` next to Edit (pre-linked to that person via `presetPerson`), and on `/crm/[id]` company detail (pre-linked via `presetCompany`).
- `f90e41d` — feat(crm): outreach activity schema — additive groundwork for outside-sales outreach logging (Oliver's flow). New `OutreachType` enum (`VISIT | CALL | EMAIL | TEXT | EVENT | DROP_IN`) and `OutreachActivity` model with: `type`, nullable `personId` / `companyId` (at-least-one app-layer constraint), `notes`, `occurredAt` (default `now()`), nullable `followUpAt` + `followUpDone Boolean @default(false)`, `createdById` FK to User. Indexes on `(followUpDone, followUpAt)` for the cheap FOLLOW-UPS DUE count, `(createdById, occurredAt desc)` for the "My outreach" filter, and per-target `(personId/companyId, occurredAt desc)` for the timeline merges on detail pages. Kept separate from the legacy `Activity` model: this is field-rep touchpoint logging with a typed channel and an optional follow-up — distinct concern, distinct table. The FOLLOW-UPS DUE card on `/crm` will aggregate open follow-ups from BOTH models in STEP 3 so the existing Activity stream doesn't lose its seat. `prisma db push` ran clean (additive only, zero DROPs).
- `f33a29a` — fix(crm): hard-skip captures whose parsed company is SirReel itself — backfill calibration surfaced "SirReel Studio Services" as the #3 most-common unmatched company string (90 unique parsedCompanyStrings, **478 capture rows** total). These are staff-signature leaks and thread-reply mentions — Haiku gets confused when a client reply quotes back our own signature block and tags `parsedCompanyString` accordingly. New `OWN_COMPANY_PATTERN = /sir\s?reel/i` in `captureConstants.ts`; hard-skip in `classifyForCapture.ts` runs immediately after the `KNOWN_VENDOR_DOMAINS` check. Retro-reclassified all 478 existing captures to SKIPPED via a two-pass script (existence checks on Person so deleted-by-prior-iteration cases don't crash): 1 fresh Person deleted (pristine FILED), 0 enrichments needed reverting (the enriched Persons were legitimate clients whose data the classifier had partially mis-tagged — their non-empty fields stayed untouched per the enrich-not-overwrite rule, so there's nothing to undo).
- `325d967` — feat(orders): push-dates flow with conflict and totals preview — deliberate "Change dates" action on the order detail page that previews every cascade BEFORE writing. New endpoints `POST /api/orders/[id]/dates/preview` and `.../apply`. Preview returns: new calendar / billable days per item with classification (inherited | custom_shifted | custom_kept), current vs projected totals via the shared `computeOrderTotals` (so no new math path — line totals recomputed via existing `computeLineTotal` against department-aware `computeBillableDays`), the delta, an asset-conflict list (any unit on this order's booking already assigned elsewhere in the new range), and a `postBooking` flag. New asset-grain helper `src/lib/scheduling/assetConflicts.ts` (`findAssetConflictsForRange`) is read-only, reuses the same overlap predicate as `getCategoryAvailability` but at unit grain, with `ignoreBookingId` so the order doesn't self-conflict. Apply re-runs the preview server-side as the source of truth (never trusts client values), rejects with 409 when conflicts exist unless `overrideConflicts: true`, then in a single transaction updates Order range + every projected item (inherited follow automatically, custom_shifted move by the offset, custom_kept untouched). Post-commit: `recalcOrderTotals` persists subtotal/tax/total; `rebaselineCadenceForOrder` clears unfired events and re-seeds the ladder against the new dates (pickup/return reminders move); an `AuditLog` row stamps `action: order.dates_pushed` with old → new ranges + per-item classification + override flag for the timeline. Modal UI on `/orders/[id]` debounces the preview, surfaces conflicts in red with an explicit "Push anyway — I've reviewed these conflicts" checkbox, per-custom-item dropdown (default `keep`), and a post-booking warning when the invoice will need adjustment. Auth: `getServerSession` on both routes.
- `da868d1` — feat(orders): flat-total mode on department discounts — mirrors the order-level flat-total UI scoped to a single department. New three-mode toggle (`%` / `$` / `Flat total`) on `DeptDiscountForm`: user enters the target department subtotal, HQ back-computes the implied FIXED discount via the shared util (`flatTotalToDepartmentDiscount({ deptSubtotal, target })` — pure `deptSubtotal − target` since per-department subtotals are pre-tax) and renders a live preview before save. Rejects targets ≥ deptSubtotal (would imply non-positive discount) or < 0; stores as `type: FIXED` with computed value and label "Flat $X dept total" — same not-pinned semantics as the order-level flat-total: adding items to the department later moves its subtotal visibly. No new math paths — both order-scope and department-scope flat-total conversions now live in `src/lib/orders/discountedTotals.ts`.
- `ea16d5a` — feat(crm): capture row email viewer + pending-review dedupe — two-part widget enhancement. (1) **Email viewer**: clicking any capture row (Needs review, Captured, or Skipped) opens a slide-over with the full email thread on the left (newest-first, From/To/date/subject/body/attachment count — bodyText only, matching the existing `ThreadDrawer` pattern from `src/components/sales/ThreadDrawer.tsx`) and the parsed payload + signals + Add / Dismiss / Undo actions on the right, so the review completes without closing the panel. New endpoint `GET /api/crm/captures/[id]` returns the capture row + the parent's `EmailMessage` + every attached child's `EmailMessage` joined and sorted by `sentAt` desc. (2) **Pending-review dedupe**: additive schema — `InquiryCapture.attachedToCaptureId` self-relation. When a new NEEDS_REVIEW would mint and a PENDING parent already exists for the same `parsedEmail` (case-insensitive), the new row is created with `attachedToCaptureId` pointing at the parent — backfill idempotency preserved (every EmailMessage still gets exactly one capture row) but the widget surfaces only the parent with a `+N on thread` badge. `dismiss` and `add` actions cascade via `updateMany` over `{ id, attachedToCaptureId: id }` so one click clears the whole thread; `add` also stamps every attached row with the new Person FK so audit reads stay clean. List endpoint filters `attachedToCaptureId: null` by default. Retro one-shot collapsed two stranded pending threads in prod (codylloyd92@gmail.com, jlable@gmail.com); Espinoza was already half-resolved by hand so only one parent remained.
- `ad66177` — fix(crm): broaden production-title tokens + vendor-domain hard-skip — calibration eyeball found two classes of miss: (1) below-the-line departments (wardrobe / stylist / HMU / hair / makeup / art department / props / locations / set dec) were under-represented in `PRODUCTION_TITLE_TOKENS` even though they are exactly the people booking us — now expanded; (2) our own service vendors (Athos Insurance, Considine & Considine CPA) get captured on `company_named` alone because Haiku doesn't mark them as cold solicitations and they don't carry a production title — new `KNOWN_VENDOR_DOMAINS` constant in `captureConstants.ts` is hard-skipped before legitimacy tests run. Retro-reclassified one Athos Insurance capture from calibration: `AUTO_FILED → SKIPPED`, Person deleted (pristine — no downstream activity). Seed list documents the date so future drift is traceable.
- `c5255ae` — feat(crm): 12-month inquiry backfill script + run report — `scripts/backfillCrmCapture.ts` runs the live capture pipeline (`src/lib/crm/captureFromEmail.ts`) over historical INBOUND mail on `info@/jose@/oliver@` from the last 365 days, scoped via `inquiryCapture: { is: null }` so it only touches unprocessed rows (idempotent — re-running picks up exactly where the last run stopped, no checkpoint file needed). When a candidate's Haiku extraction hasn't run yet, the script fires `runMessageExtractionForId` first (~5 req/sec throttle = 200 ms per call), then `captureFromEmail` explicitly for a deterministic count (vs the live path's fire-and-forget). **Calibration gate** (default ON): stops at 200 messages and prints verdict distribution, 10 sample AUTO captures with extracted fields, 5 sample SKIPs with reasons — user re-runs with `--continue` for the unattended remainder. `--limit N` caps a run for smoke-tests. **Final report**: totals (processed / AUTO / REVIEW / SKIPPED), enriched-existing vs filed-new split, role distribution from this run's `AUTO_FILED` rows, and top 20 captured company strings with NO CRM company match (candidates for manual Company creation in `/crm`). Smoke-tested at `--limit 5`: dedup correctly skipped one rfc822 sibling, classifier captured 3 (1 new Person tagged PRODUCER, 2 enriched existing), skipped 1.
- `2933344` — feat(crm): capture review widget — `CaptureReviewWidget` lands at the top of the `/crm` People tab, mirrors the `ClaimMailTriage` pattern: collapsible card, counts header (`N captured this week · M need review · K skipped`), mode toggle for NEEDS_REVIEW / AUTO_CAPTURED (audit) / SKIPPED (forensics). NEEDS_REVIEW rows expose **Add** (opens a pre-filled modal with first/last/email/phone/role/rawTitle/lastKnownProject — rep can edit before save) and **Dismiss**. AUTO_CAPTURED rows show a Link to the linked Person plus **Undo** — server reverses `AUTO_FILED` by deleting the Person (refusing with 409 when downstream rows exist) or reverses `AUTO_ENRICHED` by restoring the pre-capture values via `enrichmentLog`. SKIPPED rows are visible read-only for audit. New routes: `GET /api/crm/captures` returns rows + counts in one round-trip; `POST /api/crm/captures/[id]` dispatches `{action: 'add'|'dismiss'|'undo'}`. Person detail page (`/crm/people/[id]`) gains a capture-provenance line under the contact header — surfaces `source`, `Captured from <inbox> on <date>`, and inline pills for `rawTitle` and `lastKnownProject` when present. Stays invisible on legacy / manually-added contacts. Auth: `getServerSession` everywhere; mutations resolve to a known User so `resolvedById` is stamped on the capture for the audit trail.
- `e2f71ee` — feat(crm): production-legitimacy capture with signature harvesting — verdict classifier sits ON TOP of the Haiku `EmailMessage.extractedData` layer and writes one `InquiryCapture` row per evaluated message. **Gate**: `inbox ∈ {info@, jose@, oliver@}`, `direction='inbound'`, sender NOT @sirreel.com (Ana/claims/HR have their own pipelines). **Hard skips** (any one → SKIPPED): automated localparts (`no-reply`/`mailer-daemon`/`bounces`/`notifications`/`auto-reply`/…), Haiku `messageNature='solicitation'` or `'vendor'`, @sirreel.com authorship. **Production-legitimacy signals** (any one → AUTO_CAPTURED): role title in extracted `contact.title` matches a token list (producer / UPM / coordinator / DP / gaffer / key grip / location manager / 1st AD / …), `company` named, `jobIntent.projectName` named, sender domain matches an existing CRM `Company.website|billingEmail` (freemail jungle excluded), rental intent (`vehicleType` / equipment / dates / location / Haiku `messageNature='inquiry'`). **Ambiguous lane** (Haiku `question` / `reply` with no other signal) → NEEDS_REVIEW. **Enrich-not-overwrite**: case-insensitive email match → fill empty `phone` / `rawTitle` / `lastKnownProject`, upgrade `role` only when current is `OTHER` and parsed title maps to something specific; non-empty fields untouched; every change logged on `InquiryCapture.enrichmentLog`. **Companies**: linked on confident match (single domain hit or single exact-name hit); NEVER auto-created. **Idempotency**: `EmailMessage.id` unique key on `InquiryCapture` + `rfc822MessageId` cross-inbox dedup. Title→PersonRole mapping at `src/lib/crm/roleMapping.ts` is conservative — unclear titles bucket to `OTHER` to keep the stats strip honest. Wired into `runMessageExtractionForId` as a fire-and-forget post-extraction hook (dynamic import to keep extractor cold-start lean), so every newly-extracted inbound sales mail flows through capture automatically. STEPs 3–4 layer the review UI and the 12-month backfill on top.
- `39de542` — feat(crm): aggressive capture schema — additive groundwork for the auto-capture pipeline that turns inbound sales mail (info@/jose@/oliver@) into CRM contacts. `Person` gains four nullable provenance fields: `source` (free-form tag — `"email_capture"`, `"manual"`, `"intake_form"`), `sourceMessageId` (the `EmailMessage.id` that produced or last enriched this row), `rawTitle` (verbatim signature title preserved alongside the bucketed `PersonRole` enum), and `lastKnownProject` (most recent project / show name parsed off mail). New `InquiryCapture` model is the verdict audit trail — one row per evaluated INBOUND message, keyed on `emailMessageId` (unique). Verdict enum is `AUTO_CAPTURED | NEEDS_REVIEW | SKIPPED`; resolution enum is `PENDING | AUTO_FILED | AUTO_ENRICHED | AUTO_SKIPPED | ADDED | DISMISSED`. Parsed payload (name, email, phone, title, companyString, project) + signals JSON + nullable person/company FKs + enrichmentLog. `rfc822MessageId` enables cross-inbox dedup the same way `Inquiry` does. Companies are linked ONLY on confident match — never auto-created. `npx prisma db push` ran clean (additive only, zero DROPs). STEPs 2–4 build on this.
- `d8ca172` — fix(hr): route hr@-addressed mail to HR pipeline via routing headers (Path B) — DWD impersonation of `hr@sirreel.com` fails (it's a Google Group / alias, not a real user mailbox), so direct polling never sees those messages. Forwarding alias delivers a copy into `dani@`, where Pub/Sub fires. Added a routing-header HR branch in `src/app/api/gmail/pubsub/route.ts` immediately after `extractRoutingHeaders(headers)` and BEFORE the rfc822-dedup / `EmailMessage` write: any message whose `To/Cc/Delivered-To/X-Original-To/X-Forwarded-For/X-Forwarded-To` contains `hr@sirreel.com` short-circuits into `ingestHrEmail` and never lands in `EmailMessage` — structural isolation between HR and the rest of the inbox surfaces holds going forward. `shouldIngestHrEmail` now takes optional `routingHeaders` (Path A = direct hr@ inbox; Path B = routing-header match on any forwarded copy). `ingestHrEmail` threads `routingHeaders` through to the gate. New `scripts/replayHrEmailById.ts` replays untouched pre-Path-B stragglers in the last 30 days (or one specific `gmailMessageId`) — reads the headers off the existing `EmailMessage` row, calls `ingestHrEmail`, leaves `EmailMessage` in place (deletion risks cascading into `ClaimMail`).
- `8568189` — fix(incidents): Link company picker on incident detail — when an Incident has no `companyId` yet, the Client field renders the parsed `clientCompanyName` from the originating ClaimMail (italic, with a `parsed · unlinked` tag) and a "Link company" affordance. Click expands an inline typeahead that hits `/api/crm/companies?search=` (existing endpoint) and lists up to 8 hits; click a hit PATCHes the incident with `companyId` and the picker collapses. **NO auto-create path** — when zero matches, the picker shows "Companies can only be linked from the CRM — create one there first if needed". Spec compliance: search existing, no auto-create.
- `9bf381a` — fix(incidents): one Gmail thread = one incident — `openIncidentFromClaimMail` post-commit `propagateToThread` stamps every sibling ClaimMail row on the same `EmailMessage.threadId`; thread pre-check at the top of the helper reuses an existing sibling's Incident instead of minting. `recordClaimMail` (in `onboardFromEmail`) auto-inherits incidentId from a thread sibling when the caller didn't pass one, so new mail arriving on an existing incident's thread surfaces already-linked. `scripts/backfillThreadIncidents.ts` ran one-shot — minted `SR-INC-0003` covering the 3 Fae/Afterglow rows; `SR-INC-0004` covering the 2 CMS/Maple rows. Skips threads where rows already point at distinct incidents (manual review needed).
- `42fbb85` — fix(incidents): "View incident" link state in claim-mail widget — `GET /api/claims/mail-triage` now surfaces `incidentId` + nested `incident { id, incidentNumber, status }` per row. Widget renders an `SR-INC-NNNN` Link instead of the action button for any row whose incident is already linked (manual click in this session, prior session, the DRAFTED auto-create path, or — once STEP 2 lands — a thread-level back-link from a sibling row). The just-opened state also becomes a Link so the rep can click straight through.
- `e88a618` — feat(incidents): unified Incidents tab (claims + triage views) — `/incidents` (list with status filter chips + manual "+ New incident" modal) and `/incidents/[id]` (profile + actions + linked claims/damages/documents/original-email sections). View switcher between Incidents and Claims; `ClaimMailTriage` widget docked at the top of both. Action buttons: "Upgrade to claim" (the carrier floor lives in the modal), "Bill renter" (gracefully disabled when no Order linked), "View on order →" cross-link. Nav: Claims → Incidents (perms.claims gates identically). `/claims` redirects to `/incidents?view=claims`; `/claims/[id]` (claim detail) is unchanged.
- `a7431ec` — feat(incidents): upgrade-to-claim and bill-renter actions — `GET/POST /api/incidents`, `GET/PATCH /api/incidents/[id]`, plus action routes `POST /api/incidents/[id]/upgrade-to-claim` (carrier floor lives here: `filedAgainst` + `carrierClaimNumber` required) and `POST /api/incidents/[id]/bill-renter` (creates DamageItem rows with incidentId; gracefully 409s when no Order linked and 422s when the Order has no Booking chain — the existing LD invoice flow is unchanged and called via the existing `/api/orders/[id]/ld-invoices` route). Verified end-to-end against SR-INC-0001: upgrade minted SR-CLM-0005 with incidentId set and advanced incident OPEN → CLAIM_FILED; bill-renter gate correctly blocks on missing-Order.
- `26bb9fe` — feat(incidents): claim-mail opens incident reports — new `src/lib/incidents/openIncidentFromClaimMail.ts` (idempotent on ClaimMail.id). POST `/api/claims/mail-triage/[id]/open-incident` for the NEEDS_REVIEW + IGNORED widget action. `ClaimMailTriage` button replaces "Create claim" → "Open incident report"; shows `SR-INC-NNNN opened` inline on success. The auto-DRAFTED path in `onboardFromEmail` now creates an Incident parent in the same transaction so every NEW claim from this point traces back to an Incident — historical claims keep their null `incidentId` per the no-backfill rule. Verified end-to-end on fixture `9e9041da-...`: minted `SR-INC-0001` against Boardwalk Pictures with the parsed loss description; second call returned the same id (idempotent).
- `e06bba5` — feat(incidents): Incident model as claims/L&D hub — additive schema only. `Incident` (SR-INC-NNNN via `sr_incident_number_seq`), `IncidentSource` (EMAIL | RETURN_INSPECTION | MANUAL), `IncidentStatus` (OPEN | CLAIM_FILED | BILLED_RENTER | RESOLVED | WRITTEN_OFF). Nullable `incidentId` FK added to `InsuranceClaim`, `ClaimMail`, `DamageItem`. `ClaimDocument.claimId` becomes nullable + new `incidentId` (app-layer exactly-one constraint enforced at upload). Carrier-required floor relocated conceptually — moves to STEP 3's "Upgrade to claim" form. No existing rows affected; all FKs nullable + forward-only.

## 2026-06-10

- `1621ab2` — feat(hr): HR section with per-employee filing (allowlisted) — `/hr` employee list + `/hr/[id]` dossier (filed mail grouped by category, documents, profile) + NEEDS_REVIEW triage strip mirroring the claim-mail widget (assign-employee / set-category / dismiss / ignore). API routes (`/api/hr/employees`, `/api/hr/employees/[id]`, `/api/hr/mail-triage`, `/api/hr/mail-triage/[id]`, `/api/hr/access`) all call `requireHrAccess` — 401/403 for anyone outside the allowlist. Sidebar nav gated via `PermissionsUser.email` + `isAllowedHrEmail` — other admins see no HR entry. Both pages render a "Forbidden" fallback if the API returns 403, defense-in-depth on top of the nav gate.
- `01aceed` — feat(hr): hr@ ingest pipeline with employee filing — `HR` mode in ingestFilter (returns keep:false as safety belt); pubsub handler short-circuits hr@ BEFORE the EmailMessage write path. New `src/lib/hr/{shouldIngestHrEmail,parseHrEmail,ingestHrEmail}.ts`. Authorship gate covers staff forwards (same fix as claims/d89328e). Sonnet parse → employee match (workEmail / personalEmails / fullName fuzzy) → disposition (FILED ≥0.6 confidence + match; NEEDS_REVIEW otherwise; IGNORED on no signal). Attachments via shared `forEachInboxAttachment`. Verified zero leakage: grep across sales/claims/cron/exec/inbox surfaces returns no HrEmail references.
- `405f63f` — feat(hr): employee + HR item schema with inbox isolation — Employee model (8 staff seeded; 7/8 linked to existing Users). HrEmail / HrMail / HrAttachment tables structurally isolated from EmailMessage — the 12+ existing email-read surfaces (sales, claims, exec, inbox) all query EmailMessage so HR rows are unreachable through them by design, not by procedure. `src/lib/hr/allowlist.ts` exports `requireHrAccess()` — hardcoded constant (`wes@`, `dani@`) + `HR_ALLOWLIST` env override for emergency expansion; code-reviewed-deploy only, no DB toggle.
- `9151375` — chore(email): lift Gmail-attachment helper for HR reuse — behavior-neutral lift of the per-attachment download+upload+iterate loop out of `src/lib/claims/onboardFromEmail.ts` into the shared `src/lib/email/persistGmailAttachments.ts`. Claims path re-verified against the NEEDS_REVIEW fixture (msg `54584809-1d14-...beae4c`): same `needs_review` disposition, same parse (`Crazy Maple Studio`, `NEGOTIATING`), same reasoning. HR pipeline (next commits) builds on the same helper without duplicating Gmail+Blob plumbing.
